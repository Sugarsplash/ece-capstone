#include "lib/pin_defs.sblib"

FUNCTION temp_read()

    DIM temp_raw : temp_raw = GpioRead(TEMP_DATA)
    
    DIM temp_celsius : temp_celsius = ((temp_raw*115) / 1000) - 50
    
ENDFUNC temp_celsius